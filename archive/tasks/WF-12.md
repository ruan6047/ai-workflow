# WF-12 多 AI control-plane 與通用 DB Contract 〔🔴紅線：規則正確性〕

- 需求：ruan6047　規劃：GPT-5@Codex　分支：`ai/gpt@codex/WF-12`
- 執行：GPT-5@Codex　查核：Gemini@external-review（使用者提供；版本／工具未提供）
- worktree：`—不適用`（本 repo 現在僅此工作目錄；分支隔離已建立）
- DB：`none`；僅定義 DB Contract 範本，無實際 DB 操作
- 部署：否　環境：—　PR：—　Merge SHA：—
- 範圍：將 canonical 壓縮為不變量；新增 control-plane／DB Contract，並把 worktree、bug、模型路由的操作細節下移到按需讀取文件。各專案以 Runbook 與 `DATABASE_CONTRACT.md` 處理實作。

## 驗收

- canonical 只規範可驗證介面與不變量，不綁 DB、ORM、container、雲端供應商或模型型號。
- canonical 顯著縮短，且沒有重複操作命令與事故敘事。
- 專案可從範本定義 control-plane／DB adapter、namespace、migration runner、鎖、驗證及回滾。
- 所有 production migration 均由受保護 runner 序列化執行，且 schema/data migration 明列為紅線。
- worktree、DB namespace 與共享資源 lock 的責任邊界不矛盾。

## Log

- 07-15 需求 by ruan6047 → 要求通用規格，並讓各專案自行處理專案細節。
- 07-15 規劃／執行 by GPT-5@Codex → 改採薄 canonical：控制平面與 DB Contract 留不變量；操作細節下移範本，待獨立查核。
- 07-15 查核 by Gemini@external-review → ✅通過；無阻塞。建議將 worktree 範本的列表引用改為「§2 第 5 點」，已由執行者採納。
