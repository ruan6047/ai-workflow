---
card: WF-REDESIGN-W1
status: draft-pending-initiative
replacement_rows: [12]
---
> ⛔ 草稿·規劃產出物。各波開卡時逐字搬進卡面（甲′：規格住卡面＋spec_version）；本檔屆時封存。spec_version: 1

# 波 1 · 待審清單與開卡閘（T3）

**痛點（落差）**：開卡 5.2 張/日＞推進 3.9；open 允許直接建 issue 與 DraftIssue（實測板上有 1 張 DraftIssue）、--acceptance 可為空、--core-pain 只驗非空。
**⛔ 非射程**：不新增 wfcli 動詞；不動狀態語彙（歸切換 Initiative）；不改 canonical（W2A）。
**階段計畫**：需求 → 規劃 → 執行 → 審核 → 結案。
**級別依據**：wfcli 為狀態面唯一寫入通道＝public contract ⇒ 至少 T3；可逆（git revert）。執行 主力型（已知模式的 CLI 改動）／查核 主力型＋獨立（建議 Codex——實體效力實證）。db-scope none。
**資源**：file:cli/src/wf_cli/commands/open_cmd.py、file:cli/src/wf_cli/commands/amend_cmd.py、file:cli/src/wf_cli/card.py、file:cli/tests/、file:.github/ISSUE_TEMPLATE/
**前置（P1-18 修訂）**：板上唯一 DraftIssue item 之處置於開工時經需求方一句裁定（轉 Issue／封存／明示保留）；退場 oracle＝**raw inventory artifact**——（P1-29）producer 為 **W1 前置自產的一次性唯讀 GraphQL 查詢**（exact command：`gh api graphql` 分頁取 projectV2 items 之 `type`＋`id`＋卡ID 欄，排序依 item id，輸出 `raw-inventory-<UTC時戳>.json`＋sha256；query 版本註記於檔頭；DraftIssue ⛔ 不跳過）；expected DraftIssue count 於裁定後釘入。artifact 顯示 DraftIssue 列數 0 後，讀相容方可移除；創建封閉與遺留可讀兩軸各有測試。
**驗收**：
1. `.github/ISSUE_TEMPLATE` 收件表單上線：五條件各一欄
1b.（P1-27）清單機制上線後同批建立**四個**清單項（W2A／W2B／W3′／切換 Initiative——內容逐字取 list-items.md）；oracle＝四 URL 存在且⛔ 不在 Project #4
2. `open --from-issue <url>` 為唯一開卡路徑：直接建 issue 與 DraftIssue 兩分支移除；拒絕訊息含**跑得出**的補救指令
3. （P1-30：本卡＝card-face JSON schema **唯一 owner，v1**）表單新欄以 fenced JSON 承載：專用 sentinel（⛔ 不與既有 resource-claims 區塊混用、⛔ 不以「找到一個 JSON fence」定位）、schema_version 鍵、keys/types 封閉定義（階段計畫／級別依據／清單收斂）、同類區塊唯一性；tests 釘 writer→reader round-trip／legacy 無區塊 fallback／同類兩區塊拒收／malformed 拒收／unknown version 拒收／與 resource-claims 共存
4. 驗收條件改必填（≥1）；--needs-deploy 移除
5b.（P1-26，唯一 owner＝本卡）既有 `amend` 動詞擴充 feature／routing 更新旗標（⛔ 非新增動詞；依 CLI 增量原則走三層評估）：write-set 含 Issue title＋Project item title＋`功能` 欄＋routing 行，round-trip test 讀回驗證；以此通道同步 #177 的 routing（移除「不可逆狀態遷移」句改「W2A canonical T4 紅線」）與 title／功能——**退場 oracle：三欄皆含「四波五卡」且零「五波施工」、routing 零「不可逆」字樣**
5. （P1-07 修訂）撤銷程序的**規則條文化歸 W2A**（requirement.md 生效時已含 ADD 撤銷邊與固定動作）；本卡僅於收件模板說明欄註記「降回清單＝PM 執行 deleteProjectV2Item＋轉移留言」，⛔ 不新增 CLI 動詞、⛔ 不先行生效任何 stage-rule
**驗證**：對缺表單欄位的 --from-issue 實跑拒收（訊息含補救）；對舊路徑實跑確認移除；pytest 全綠；交付報告附 CLI 淨 LOC 變化（diff 產生）；⭐ W2A 的清單項用本機制實際升級一次＝驗收即實戰。
