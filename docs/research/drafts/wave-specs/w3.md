---
card: WF-REDESIGN-W3
status: draft-pending-initiative
replacement_rows: [2, 4, 7, 10, 13, 14]
---
> ⛔ 草稿·規劃產出物。各波開卡時逐字搬進卡面（甲′：規格住卡面＋spec_version）；本檔屆時封存。spec_version: 1

# 波 3 · 狀態面 cutover（T3+，刻意停機）

**痛點（落差）**：交付狀態 15 值仍承載兩軸；Log 佔 body 74.8%；卡面機讀靠自寫解析（corpus 至少五個根因）；157 張終態卡佔活卡視圖。
**⛔ 非射程**：不動 cpbl 舊卡內容（波 4）；不改 canonical 條文（僅移除「尚未切換」標記）。
**階段計畫**：需求 → 規劃 → 執行 → 審核 → 部署（此處＝cutover 本身）→ 結案。
**級別依據**：不可逆（刪 option id）＋唯一寫入通道大改 ⇒ T3+。執行 高階型／查核 跨家族建議。db-scope none。
**資源**：file:cli/src/、file:cli/tests/、file:scripts/（doctor 抽出目的地）、file:cpbl-analytics/scripts/roadmap_lines.py、file:cpbl-analytics/scripts/review_prompt.py、file:cpbl-analytics/scripts/workflow_ledger.py、file:cpbl-analytics/tests/（狀態語彙檔遷移各自 PR；`file:cpbl-analytics/` 前綴＝跨 repo 邊界慣例——P1-13）
**前置（⛔ 不可逆動作的閘門——P1-20 critical 修訂）**：
- **W2B 已終態**（硬依賴鏈）
- ⭐ **完整 restore rehearsal 於拋棄式 Project 實跑**：snapshot 擴充至涵蓋受影響 item ID、field／option ID 與名稱、逐 item 欄位值、view／filter 與封存集合；演練含逐步 inverse 指令、每步 checkpoint 與 read-back oracle
- ⭐ **明示步④（刪 option）起⛔ 不是 rollback**——平台刪 option 不可原樣復原 ⇒ 該段為 **forward-repair**；凍結前需求方核可 forward-repair 計畫與**停損點**；rehearsal 未實證⛔ 不得進停機步②以後
- ⚠️（P1-17）「160 處」「157 張」為 2026-08-30 量測——開卡時同一次 artifact 重量並釘字面，驗收引 artifact ⛔ 不引本句；前張終態 SHA 與 Project snapshot hash 開卡時寫成字面基線
**停機序**：引用決議 §十唯一定義（前置 3 項＋停機七步①–⑦），⛔ 不自報步數。#133 於⑤時改判被吸收。
**同捆（凍結前完成）**：doctor 抽出成獨立腳本＋CI job；≥37 則拒絕訊息補「跑得出」的補救（補不出的列為裁定候選呈需求方）。
**驗收**：
1. 停機七步①–⑦逐名留痕；任一失敗 sys.exit(1)⛔ 不得部分成功宣告完整
1a.（P1-14，本卡唯一 owner）資源比對兩行為落地：`file:` 前綴包含＋`db:` 別名正規化（local|prod），各附正反例測試（前綴命中／非前綴不誤殺；已登記別名命中／未登記不猜）
1b. 逐 row 可判驗收——row 2：交付狀態欄選項逐字＝新 10 值且 0 列殘留舊值；row 4：`git grep "尚未切換" -- AI_WORKFLOW.md` 零命中；row 7：`部署狀態` 欄不存在於 Project 欄位清單且 `DEPLOYMENT_TRANSITIONS` 移出 CLI；row 10：`--spec-dir` 自 open 移除且 help 零提及；row 13：wfcli 對內建 Status 的寫入呼叫點＝0；row 14：`📥Backlog` 於新選項集不存在
2. 新語彙上線後：轉移記錄由 5 寫入點寫入固定留言；卡面現值＝轉移記錄末行 to（不變量實跑）
3. 封存 157 張可逆驗證（抽 1 張 unarchive 再 archive）
4. 停機期間本卡自身轉移由 PM 手寫留言（卡點 B）
**驗證**：遷移對帳表（211 列 old→new 全對照）；pytest；污染符 grep；cpbl origin/main 9 檔同步遷移各自 PR。
