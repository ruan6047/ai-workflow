---
card: WF-REDESIGN-W3
status: draft-pending-initiative
replacement_rows: [10]
---
> ⛔ 草稿·規劃產出物。各波開卡時逐字搬進卡面（甲′：規格住卡面＋spec_version）；本檔屆時封存。spec_version: 3

# W3′ · CLI 內部改造（T3；⛔ 零看板語彙變更、⛔ 零 cpbl 接觸）

**痛點（落差）**：Log 佔 body 74.8%（2026-08-29 量測，開卡時 artifact 重量）；卡面機讀靠自寫解析（corpus 至少五個根因）；doctor 3,039 行使 CLI 偏離「祕書最小集」；57 則拒絕訊息僅 ≤20 含可跑補救；find_conflicts 逐字比對漏前綴與別名。
**⛔ 非射程**（乙′ 解耦）：⛔ 不動看板任何欄位名／選項／封存（歸切換 Initiative）；⛔ 不碰 cpbl 任何檔；⛔ 不動 deploy-state／deploy-declare（隨欄位退位歸切換）；⛔ 不新增 wfcli 動詞；rehearsal 工具歸切換 Initiative。
**階段計畫**：需求 → 規劃 → 執行 → 審核 → 結案。
**級別依據**：wfcli＝唯一寫入通道（public contract）⇒ T3；全部可 git revert（零不可逆）。執行 主力型／查核 主力型＋獨立；需求方閘門適用。
**前置**：W2B 已終態（硬依賴鏈）。
**資源**（全 aiwf）：file:cli/src/、file:cli/tests/、file:scripts/（doctor 抽出目的地）、file:.github/workflows/ci.yml（P1-21）、file:templates/database-contract.md（P1-14 別名表 owner 檔）
**驗收**（動態數字開卡時由同一次 artifact 重量並釘字面；前張終態 SHA 開卡時寫成字面基線——P1-17）：
1. （P1-33 critical，需求方 2026-08-31 裁定**乙′：一事件一留言**——GraphQL updateIssueComment 僅 id+body、REST 無文件化條件更新，固定留言無 CAS 必丟並行更新）persistent Log writer sink 封閉全集＝**7 個**（open 初始 Log＋assign／amend／checkpoint×2／review／handoff；具名排除 doctor 非持久化 probe；inventory test 以 predicate 實跑、多一少一即紅）。每事件新建一則留言；**保證模型＝實體 at-least-once／邏輯 exactly-once**（GitHub 建立留言無文件化伺服端冪等——AddCommentInput 僅 clientMutationId/subjectId/body）：op id 於**邏輯操作開始時**產生並**跨 retry 重用**；writer retry 前**重掃**既有留言；reader 按 op id 分組——同 hash collapse＋warning、**不同 hash＝corruption fail-closed**；索引留言只由 canonical deduped event set 重建（純投影、丟更新自癒⛔ 不承重）。部署**兩階段釘死先後**：①dual reader 上線＋backout rehearsal 通過 → ②才切 writer epoch；回退＝關新 writer 保留 dual reader，或依明列步驟把 epoch 後事件 materialize 回 body；rehearsal 以事件 op id 集合＋內容 hash 證⛔ 不少一筆；舊 body Log 原地凍結⛔ 不搬
2. 卡面機讀 fenced JSON：**只擴充／消費 W1 的 v1 schema**（版本升級依 W1 定義之規則，⛔ 不另立 schema）；讀取端雙路徑（舊卡切割 no-op 已驗）；--spec-dir 移除（row 10）
3. （P1-28 定案：**轉薄**⛔ 非移除——AGENTS／dispatch-package／handoff-contract／CONSUMER_CONFORMANCE 均直接消費 `wfcli doctor`）：邏輯抽至 scripts/＋ci.yml 具名 job；`wfcli doctor` 保留名稱／旗標／rc／輸出契約、委派至抽出腳本；加現行指令 vs 新 CI job 的等價 round-trip 測試；淨 LOC 變化由 diff 產生附交付報告
4. ≥37 則拒絕訊息補「跑得出」補救（開卡時 artifact 重列全集）；補不出的列為裁定候選呈需求方
5. （P1-14 唯一 owner；P1-34 定稿）assign 交集檢查三項行為：
   (a) 候選母體 old→new（owner 非佔位 → 有 branch 或 worktree）＋正反例＋兩判準差集 inventory test；
   (b) `file:` 前綴包含——component sequence／NFC／casefold **逐字依 WF_RESOURCE_WRITESET1 既定語意**；tests＝component boundary（`a/bc` ⛔ 不被 `a/b` 命中）／NFC 等價命中／casefold 等價命中／真非前綴負控；
   (c) `db:` 別名——封閉三格：**已登記別名 → 正規化命中；未登記 → 按字面＋stderr 警示；registry 載入或解析失敗 → 在任何遠端寫入前拒絕 assign（fail-loud）**。⛔ 前綴測試不視為涵蓋母體。
6b.（P1-35 機制 owner）pitfalls 逐條 note roster＋handoff gate＋CLI 列印含編號三層清單，tests 同批。**gate 消費者與 pass/fail 封閉**：回應清冊的 ID 集合須與本次 CLI 印出的三層集合**相等**；缺 ID／多 ID／值域外／`不適用` 缺原因／`發現` 缺處置——五種各自拒收（各有 fixture）；**PM 的派審詞與結案報告走同一 validator**；W2A 的「未生效」標記保留至本組 tests 全綠才啟用——決議 §三之二
6. snapshot 補欄（階段／簡介／規格節）＋（P1-29 修訂）**把 W1 既有 raw-inventory artifact 的 schema 產品化進 snapshot**（⛔ 不得稱本卡後置產物為 W1 Gate 的來源——producer 在 W1 前置）
**驗證**：pytest 全綠；污染符對 post-image grep；交付報告附 CLI 淨 LOC。
