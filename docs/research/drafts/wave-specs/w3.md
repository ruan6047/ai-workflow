---
card: WF-REDESIGN-W3
status: draft-pending-initiative
replacement_rows: [10]
---
> ⛔ 草稿·規劃產出物。各波開卡時逐字搬進卡面（甲′：規格住卡面＋spec_version）；本檔屆時封存。spec_version: 3

# W3′ · CLI 內部改造（T3；⛔ 零看板語彙變更、⛔ 零 cpbl 接觸）

**痛點（落差）**：Log 佔 body 74.8%（2026-08-29 量測，開卡時 artifact 重量）；卡面機讀靠自寫解析（corpus 至少五個根因）；doctor 3,039 行使 CLI 偏離「祕書最小集」；57 則拒絕訊息僅 ≤20 含可跑補救；find_conflicts 逐字比對漏前綴與別名。
**⛔ 非射程**（乙′ 解耦）：⛔ 不動看板任何欄位名／選項／封存（歸切換 Initiative）；⛔ 不碰 cpbl 任何檔；⛔ 不動 deploy-state／deploy-declare（隨欄位退位歸切換）；⛔ 不新增 wfcli 動詞；rehearsal 工具歸切換 Initiative。
**階段計畫**：需求 → 規劃 → 執行 → 審核 → 結案。
**級別依據**：wfcli＝唯一寫入通道（public contract）⇒ T3；全部可 git revert（零不可逆）。執行 主力型／查核 主力型＋獨立；需求方閘門適用。
**前置**：W2B 已終態（硬依賴鏈）。
**資源**（全 aiwf）：file:cli/src/、file:cli/tests/、file:scripts/（doctor 抽出目的地）、file:.github/workflows/ci.yml（P1-21）、file:templates/database-contract.md（P1-14 別名表 owner 檔）
**驗收**（動態數字開卡時由同一次 artifact 重量並釘字面；前張終態 SHA 開卡時寫成字面基線——P1-17）：
1. （P1-22 封閉全集）Log 新條目改寫固定留言的寫入點＝append_log_line 呼叫點全集，實測 **6 處**（⛔ 非先前所稱 5）：assign_cmd、amend_cmd、checkpoint_cmd×2、review_cmd、handoff_cmd——附 inventory test（呼叫點集合＝此六者，多一少一即紅）；舊 body Log 原地凍結⛔ 不搬
2. 卡面機讀改 fenced JSON；讀取端雙路徑（舊卡切割 no-op 已驗）；--spec-dir 移除（row 10）
3. doctor 抽出：scripts/ 獨立腳本＋ci.yml 具名 job；wfcli 內 doctor 移除或轉薄——淨 LOC 變化由 diff 產生附交付報告
4. ≥37 則拒絕訊息補「跑得出」補救（開卡時 artifact 重列全集）；補不出的列為裁定候選呈需求方
5. （P1-14 唯一 owner，三項行為）assign 交集檢查改造：(a) **候選母體判準 old→new：owner 非佔位字串 → 有 branch 或 worktree**（正例：有 worktree 的活卡入選；反例：owner 有值但無分支者不再入選；inventory test 對快照母體實跑兩判準差集）；(b) file: 前綴包含；(c) db: 別名正規化。⛔ 前綴測試不視為涵蓋母體——兩者不同層。別名登記表住 templates/database-contract.md（schema＝別名→正名兩欄表；載入失敗或未登記＝按字面比對＋stderr 警示⛔ 不猜）；測試含前綴命中／非前綴不誤殺／已登記別名命中／未登記不猜／大小寫不正規化即不命中
6. snapshot 補欄（階段／簡介／規格節）＋（P1-18）raw inventory artifact：GraphQL 全 items 含 content type／item ID／卡ID 三欄（DraftIssue ⛔ 不跳過）——W1 退場 oracle 的資料源
**驗證**：pytest 全綠；污染符對 post-image grep；交付報告附 CLI 淨 LOC。
