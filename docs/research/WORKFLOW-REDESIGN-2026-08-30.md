# 工作流重整決議紀錄（2026-08-29 – 30）

> **性質**：需求方（ruan6047）與 PM（Claude Opus 5@Claude Code，session `cc0a7952-07a5-4978-8d03-8b5f48fbc690`）逐題共同設計的定案紀錄。B1 記錄文件、依 `a8f6f4c`（WORKFLOW-REVIEW-2026-08-04）先例直接 commit。
> **優先權條款**：施工卡與 canonical 衝突時，**以本紀錄為準**；下表所列節次在對應波改寫前**視為過時**。（形狀沿用 canonical 檔頭「程式碼與文件衝突時以程式碼為準」與 §0.1「本節定義目標狀態，尚未切換」先例。）
> ⛔ 本紀錄非生效條文。生效走四波五卡施工（切換與 cpbl 移植另歸切換 Initiative）；`drafts/` 下全部檔案 `status: draft-pending-initiative`。

## 一 · 取代清單（14 列，每列唯一 owner；P1-04 修訂 2026-08-30）

| # | 被取代 | 取代者 | 唯一 owner | 狀態 |
|---|---|---|---|---|
| 1 | canonical §0 十五值單欄序列（條文） | 本紀錄 §三（8×10），帶「尚未切換」標記 | W2A | 待做 |
| 2 | 看板實際狀態語彙（15 值） | 8×10 cutover | 切換 Initiative | 待開 |
| 3 | canonical §1 角色表（7 角色） | 本紀錄 §六（6 角色） | W2A | 待做 |
| 4 | canonical §0.1 過渡註記 | cutover 後移除 | 切換 Initiative | 待開 |
| 5 | aiwf `MODEL_ROUTING.md`「記入 claim 事件」 | assign 旗標＋信封行 | — | ✅ 已完成（9bb9cba） |
| 6 | cpbl `docs/MODEL_ROUTING.md` 同句 | 同上 | 切換 Initiative | 待開 |
| 7 | `部署狀態` 欄與 `DEPLOYMENT_TRANSITIONS` | 退位成部署階段產出物；強制力移 `stage_next[部署]` | 切換 Initiative | 待開 |
| 8 | aiwf `TASKS.md` | 墓碑→移除；入口改查詢指令 | — | ✅ 已完成（26ba48d＋9bb9cba） |
| 9 | cpbl `docs/TASKS.md`＋CLAUDE.md 路由 | 同上 | 切換 Initiative | 待開 |
| 10 | `open --spec-dir` | 甲′：規格住卡面＋spec_version | W3′ | 待做 |
| 11 | 57 份 cpbl spec 檔 | 封存 | 切換 Initiative | 待開 |
| 12 | `--needs-deploy` 旗標 | 開卡表單「階段計畫」 | W1 | 待做 |
| 13 | 內建 `Status` 欄 | 已退出 view（✅）；wfcli 寫入移除隨部署欄退位 | 切換 Initiative | 部分完成 |
| 14 | `📥Backlog` 狀態值 | 狀態「待辦」＋待審清單 | 切換 Initiative | 待開 |

## 二 · 污染符清單（查核者對施工卡 diff 逐字 grep；命中即 finding；⚠️ 下界——抓字面不抓換句話說）

`claim 事件` `claim event` `events.jsonl` `workflow_ledger` `control-plane`（新規則文脈） `📥Backlog`（新規則文脈） `⏳待執行` `🚧進行中` `🔍待查核`（新規則文脈） `部署狀態`（作為狀態軸） `needs-deploy` `spec-dir` `Discovery lead`（角色表文脈）

## 三 · 階段與狀態（8 × 10）

- **階段 8**：需求 → 研究* → 規劃* → 執行 → 審核 → 部署* → 維護* → 結案（* 可跳過；跑哪些由開卡「階段計畫」宣告）。合併與收尾⛔ 不是階段（判準：階段是有人做判斷的地方）。需求固定動作＝**撤銷**（`deleteProjectV2Item`，卡回清單、卡ID 保留、iteration 延續）；結案固定動作＝**封存**（`archiveProjectV2Item`）。
- **狀態 10 bit**：通用 7（待辦／進行中／待確認／完成／退回＋正交：阻塞／升級）＋專屬 3（研究·**不可判定**、維護·**運行中**、結案·**停止**）。專屬狀態判準（寫成規則）：換成通用狀態會不會丟失**行為**差別。維護／失效不合格（＝待辦）。
- **轉移**：`stage_next[階段][狀態] = BASE | ADD & ~REMOVE`，各階段規則檔只寫 delta；可達性檢查跑在規則檔變更時。bitmask 是 CLI 端驗證⛔ 非看板約束。
- **停止**＝結案專屬終態、⛔ 無出口；裁定必含決策／原因／**可證偽復活條件**；復活＝開新卡。阻塞＝等得到，解除回轉移記錄 `from`。
- **迴圈 5 步**：① CLI 印三層注意事項＋清冊 ② PM 派 ③ 執行者交回 ④ 多輪有序 R1 前提→R2 射程→R3 內容→R4 影響面（R1 不過⛔ 不跑後面）⑤ 過／退回／退回上一階段。退回計數**按輪**記，同輪第 3 次自動升級。
- **升級四選一**：①換人（本階段/進行中）②退回上一階段（規格只能在規劃改）③停卡 ④退回無效（撤銷該次退回）。「改規格」⛔ 不是值。

## 三之二 · 注意事項回應清冊（需求方 2026-08-31 裁定甲：三層全逐條＝SOP 執行證明）

**owner 接線（P1-35）**：條文於 W2A 隨 stage-rules 生效但**標「目標、尚未生效——機制生效於 W3′」**（⛔ 不啟用無 writer 規則，沿 §0.1 先例）；**機制 owner＝W3′**（pitfalls 由族級 roster 擴逐條 note roster＋handoff gate＋含編號清單列印，tests 同批）；**範本 owner＝W2B**（派工包／交付報告範本含清冊欄）。

注意事項全編號化（`F-<階段>-NN`／`P-`／`T-`，W2A 落檔時做）；③ 交回必附回應清冊——對 ① 印出之三層編號清單**逐條**回應，三值＝已遵循／不適用：<原因>／發現：<處置>；④ 對格數與值域（機械對照；CLI 印含編號清單＝提供資訊類）；**格數不符＝退回**。PM 產出（派審詞／結案報告）同罩（檢核清冊見 pm-conduct）。防打勾退化＝沿用踩坑 digest 門檻＋查核抽驗。L0 措辭：「stage-rules/＝八份 SOP，① 印給你、③ 逐條回」。


## 四 · 待審清單與開卡

- 清單項＝不在 Project #4 的 issue（兩 repo 各自）；卡＝在板 issue。`open` 只吃 `--from-issue`，封直接建 issue 與 DraftIssue 兩路。清單項⛔ 不配卡ID、⛔ 不掛 sub-issue（auto-add workflow 開著）。清單當墓地可接受。
- **收件五條件**（PM 只判流程）：出處可指／是觀察不是結論（⭐ 唯一需第二 PM）／查重留痕（列關鍵字＋命中）／屬哪 repo／提案者身分（GitHub 帳號＋session ID＋訊息定位；⛔ 不填模型名——harness 已寫入 transcript）。
- **開卡表單**：A 逐字繼承（痛點原文／出處／身分／repo）＋ B 新增（卡ID·功能·⛔非射程·**階段計畫**·**驗收≥1 逐條可追溯**·驗證項目·**清單收斂**（開卡宣告、結案核對）·服務目標·鏈深≤2·級別＋**級別依據**三子問·能力層級×2＋理由·db-scope·資源宣告）。
- 級別**單向門**：升自由；降須需求方裁定留痕。分級表兩層 DI（框架＝canonical「變更級別」段（§0 前言，逐字「取風險、影響範圍與可逆性的最高者」）表格化；專案層只能加嚴）。

## 五 · 留痕

- **轉移記錄**：`時間 from(階段/狀態) to 輪次`，⛔ 無第五欄（升級出口例外多一值）。不變量：卡面現值＝最後一行 to。
- **Log 移出 body**（P1-33 十二輪定稿，⛔ 原「一則固定留言」模型已因無 CAS 廢棄）：**一事件一留言**（實體 at-least-once／邏輯 exactly-once，envelope 凍結重用）＋索引留言＝純投影；部署兩階段 flags 可回退；細節以 W3′ spec 為準。舊 body Log 原地凍結。
- **甲′ 規格居所**：卡面單一居所＋`spec_version` 必 bump；基線＝版本號（WF-17 查核硬退沿用）；稽核＝`userContentEdits`（實測 diff 欄＝全文快照）。

## 六 · 角色（6）與授權

需求方（該不該做＋升級裁定＋**結案階段的 ④**）／人工查核（同人異帽：前提對不對）／PM（流程＋狀態唯一 writer；⛔ 不判內容）／第二 PM（Codex；PM 自產物的檢查；有完整寫入通道——身分靠 session ID＋transcript 核對）／執行者／查核者（跨家族；`core_pain_resolved` 否決權）。

- 同卡同輪一人一角。PM 兼執行：**T0/T1 可、T2+ ⛔ 不可**（風險軸⛔ 非難度軸）。
- **丙 直行授權（2026-08-30 需求方裁定）**：APPROVE＋④ 過 ⇒ PM 直行 merge→release→cleanup；四停下條件（blocking 未清／CI 非綠或狀態不符／BEHIND 衝突／T4）；**對價＝結案報告閘**：一屏七段（含失誤登記與未驗清單**逐字**），結案 ④＝需求方，可退回補驗。全文見 drafts/pm-conduct。
- 研究高階型：查核＝可重跑＋**≥3 不同族角度對抗性反測**（不適用附原因）；執行者必附可重跑 harness。交付報告缺席＝③ 未完成（PM ⛔ 不代寫自評；失聯出口＝重做或標明「複驗報告」）。
- 交接文件五份：派工包／交付報告／派審詞／裁決／狀態變更裁定單；共同信封（卡與身分＋**已知未驗項逐項＋各自原因、分驗不了／沒去驗／刻意不驗三種（承接 canonical §6.4.2）**＋身分自述＋機械指令＋**實際模型 vs 建議層級行**）。root_cause_id 自由文字、派審詞必列前輪。

## 七 · CLI 射程與拆解

印清單＋確認有沒有填＋GitHub 機械操作；⛔ 不判內容。**增量原則（需求方 2026-08-30 確認）：⛔ 不是不能加、是⛔ 不無限膨脹——動詞新增須需求方裁定；旗標／欄位／資訊輸出走三層評估；「提供資訊」類收錄最寬。**值域降為規範（PM 對照）。拒收點三層：執行者提出→PM 判「訊息有沒有**跑得出**的補救」→⛔ 不可補上呈。`doctor` 抽出成 CI job。卡面機讀改 fenced JSON（W3′）。實測：57 則拒絕訊息 ≤20 過門檻，≥37 待補＝裁定候選自浮清單。資源檢查：母體改「有分支或 worktree」（29%→88%）、比對改路徑前綴包含（假陰性 7 對＝命中 7 倍）。

## 八 · 注意事項（定稿；各階段條數見下、⛔ 不另記總數——總數是會漂的第二居所。DI 三層：任務→專案→框架；累加⛔ 不覆寫；升遷門檻＝3 張卡）

**需求 15**：清單項升級（--from-issue）／痛點逐字沿用／卡ID 升級才配／查重列關鍵字／清單收斂宣告／同族第三張改窮舉卡先量母體／⛔非射程必寫／驗收≥1 可追溯／⛔不寫解法／鏈深≤2 路由一次填齊／可判「現在不做」降回清單／PM 自產物由第二 PM 檢／**grilling 產出物檢查（Initiative/T4：質詢紀錄存在、存活反駁入⛔非射程）**／**級別依據三子問**／**階段計畫只寫要跑的、節名逐字**。
**研究 14**：先量母體／不窮舉時報母體與掃量（紅數＝下界）／實跑⛔不讀碼推論／量在 origin/main／驗證器 import／不顯著⇒不可判定⛔不得引為反證／證不出來≠沒有／關鍵字沒命中⛔非證據／推翻在宣稱自己的母體上做／「全部」附窮舉證據由 artifact 產生／討論形狀⛔非送審形狀／先讀卡上留言／**先搜既有解法（列出搜過什麼；引用照 §6.1 四約束）**／**高階型必附可重跑 harness**。
**規劃 8**：驗收可追溯痛點／⛔不拿全 repo 現況當本卡標準／檢查非零資訊（先講什麼會推翻它）／間歇缺陷預先登記可證偽預測／守衛基線釘死 SHA 字面／指定誰跑（合併結果上）／同族第三輪＝形狀錯（封閉集合、天花板跳出法）／顆粒度三層（試跑慣例）。
**執行 12**：worktree 路徑紀律／（並行共用 checkout 時）逐檔 add、SHA rev-parse／⛔不截斷輸出／rc=0⛔非成功看狀態／宣告成功前核執行識別碼／驗證器 import／複驗用會通過的樣本／算術不可能最先響／刻意行為就地留註解／修過期最易留新過期／交付物寫事實⛔不寫狀態／失誤登記與未驗清單逐項⛔不摘要⛔不加緩和語（＋「全部」附 artifact 窮舉證據）。
**審核 9**：基線 merge-base 釘死／轉手 finding＝背書／self_run 實跑 rc 分開取／core_pain_resolved 否決權／證據逐字轉錄／⛔不代改／root_cause 對照前輪第三輪升級／守衛在合併結果上跑／裁決含身分自述、有通道自己寫回。
**結案 7**：合併⛔非結案／不帶 --cleanup 的 release 寫明理由／終態才釋放資源／清單收斂核對／補帳⛔不代寫他人 finding／對帳 merge-base --is-ancestor 對生產指標／開工前 fetch 防並行。
**部署 0／維護 0**：留空標⛔ 未寫，累積實例後補。

## 九 · 入口文件（乙′）

L0＝canonical §1（波 2 改寫後）＋§2＋專案一分鐘心智模型（~42 行）；§1/§2 移 §0 前；入口⛔ 不指向會變的東西（活卡＝查詢指令）；刪三腐爛自述（「短版」／「最後核實 2026-06-22」／TASKS.md 指向）。

## 十 · 四波五卡實施與切換移交

**四波五卡（乙′ 解耦定案 2026-08-30）**：W0 conduct 生效（**T4**——conduct 即規則紅線；審核滿足＝需求方本 session 逐條確認之使用者 sign-off；執行⛔ PM 不可兼）→ W1 清單＋open＋表單（**T3**）→ **W2A canonical 本體＋stage-rules 生效＋tier-rules（T4 紅線，跨家族審）** → **W2B 配套：交接範本（含 contract templates 改寫）／L0／舊模板清理（T3）** → **W3′（＝卡 WF-REDESIGN-W3 之簡稱）CLI 內部改造（T3，⛔ 零看板語彙變更、⛔ 零 cpbl 接觸）**：Log→留言、卡面 fenced JSON、doctor 抽出、37 則拒絕訊息、find_conflicts 前綴＋別名、snapshot 補欄。
**硬依賴鏈：W0 → W1 → W2A → W2B → W3′**（前張終態才可開下張）。本 Initiative 交付＝**框架就緒、⛔ 不切換**——canonical 新 §0 帶「尚未切換」標記，看板續跑 15 值舊語彙。

## 十之二 · 切換 Initiative（待開；本 Initiative 結案條件含其清單項建立）

cutover 與全部不可逆／跨 repo 內容移交於此，屆時與 cpbl 移植同組：
1. 看板切換：改名（交付狀態→狀態，view filter 同步）→ 選項 15→10 → 終態卡封存（數量屆時 artifact 重量）——凍結宣告含 **Project 寫入全域 freeze**，⛔ 不得以卡序列代替
2. **P1-20 全文於此生效**：拋棄式 Project 完整 restore rehearsal（item／field／option／view 全涵蓋、逐步 inverse、read-back oracle）；**刪 option 起⛔ 非 rollback＝forward-repair**，凍結前需求方核可計畫與停損點；rehearsal 未實證⛔ 不得進切換步②以後；rehearsal 工具＝拋棄式腳本⛔ 不入 wfcli
3. cpbl 側：9 檔語彙相容、review_gate_inventory 的留言 Log 缺口、stub 摘要同步、DATABASE_CONTRACT 別名表 cpbl 副本、submodule 指標 bump（現凍結於 f207d2ec——bump 即移植第一步）、cpbl TASKS.md／MODEL_ROUTING 清理（原 W4 項）
4. 原 W4 全部內容：36 張舊卡逐張處置、57 份 spec 檔封存、13 個 db: 宣告正規化、15 張 write+ 補宣告、舊分支欄刪除——數字開卡時 artifact 重量
5. deploy-state／deploy-declare 動詞與內建 Status 寫入移除（隨部署狀態欄退位）
6. **回顧觸發移交於此**：cutover 後第 30 張常態卡結案；fail-safe 2026-10-31 不變——屆時未開切換 Initiative 亦強制檢討

父卡跑迴圈⛔ 不做事；子卡只在父卡規劃誕生；父卡⛔ 不宣告 file 資源（⚠️ 皆無機械執行處，靠 PM 紀律＋查核）。
立即生效（純流程）：丙 授權＋結案報告閘／交付報告缺席＝未完成／三反測／信封／轉移記錄手寫。回滾：本 Initiative **零不可逆動作**（全部可 git revert）；看板側復原能力歸切換 Initiative 的 restore rehearsal——⛔ 每日 snapshot 只是對帳投影⛔ 非回滾通道（P1-20）。

## 十一 · 已執行（2026-08-29–30，均回讀驗證）

兩 view 可見欄位改自訂欄（移除內建 Status）／刪 `部署狀態` 的 `🏁完成`（0 列 0 writer）／停 7 卡（#54 #55 #56 #60 #115 #84 #86，各附可證偽復活條件）／阻塞 15 卡（解除條件＝清單機制上線）／#165 跨家族查核→升級②→R4→merge `52780f6` 結案。

## 十二 · 資源管理補議（2026-08-30 補討論定案）

1. **覆蓋規則（優先）**：`db_scope ∈ {write, schema, data-migration}` ⇒ 資源宣告必含 ≥1 個 `db:` 資源；PM 開卡 ④ 對照（同級別依據形狀），⛔ 不進 CLI 硬拒。依據：實測 22 張寫入卡僅 7 張有 `db:` 宣告（68% 對互斥檢查隱形）。既有 15 張未宣告者歸波 4。
2. **環境名（丙）**：regex ⛔ 不動（只驗形狀）；`DATABASE_CONTRACT` 明文封閉枚舉 **`local`｜`prod`** ＋別名表 `production→prod`、`dev→local`（需求方 2026-08-30 核准；cpbl 實際僅兩顆庫：本機 5433 與 VPS 生產）；`find_conflicts` 比對前依已登記別名正規化並以測試釘住（現別名測試為 0）；13 個既有宣告波 4 正規化。⚠️ 收緊 grammar 被否決——單向門（曾鎖死 33 張卡 amend），且方向違反「值域歸規範、PM 對照」。
3. **生命週期三時點**：開卡粗宣告（目錄前綴可）→ 規劃 ⑤ 依規格 amend 精確化（⚠️ `--resources` 整份取代——先讀現值、整份重列；寫進規劃階段注意事項）→ assign 交集檢查。
4. **型別⛔ 不擴**：`port:`／`container:` 實測使用量 0。
5. 另已定（前議）：交集母體判準改「有分支或 worktree」（29%→88%）；`file:` 比對改路徑前綴包含（假陰性 7 對＝命中 7 倍）。**行為變更的唯一 owner＝W3′**（P1-14），驗收含正反例測試。
6. ⚠️ 更正：memory 舊載「DATABASE_CONTRACT §3 與 regex 不一致」已過期——現行契約與 regex 一致且自引 regex 原文。

## 十三 · 未定／未查

未定：分級表專案層級別數字／部署維護內容／舊卡處置細節（切換 Initiative）／`🚧進行中` 等廢止值隨切換 Initiative 消滅。未查：144 拒絕點餘 118 未分類／63 條中已在 canonical 者未逐條查／card.py 砍半是估計／留言 65,536 上限通說／刪有值選項未實測／`決策佇列` writer 未查／26 張 T2+ 繞規劃豁免未查／舊分支欄未刪。
