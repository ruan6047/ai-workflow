# ai-workflow 藍圖

> 需求方 2026-08-12 裁定的目標排序與卡片分級。**本檔是「為什麼做這張卡」的唯一來源**；
> 卡片本身寫「怎麼做」。開新卡前先在此找到它服務哪個目標——找不到就不該開。

## 0. 目標排序

需求方 2026-08-12 逐字裁定：

> 應該是**可稽核的內容**跟**防止低級事故**為主，其他是有最好但非必要。

| 序 | 目標 | 判準 |
|---:|---|---|
| 1 | **防止低級事故** | 有機械執行者會擋下它。沒有執行者的偵測器**不算**達成 |
| 2 | **可稽核的內容** | 事後能從留痕重建「做了什麼、依據是什麼」。**不含身分**，見 §1 |
| 3 | 其他治理精緻化 | 有最好，非必要。降級為 Backlog，有餘力再做 |

### 開卡前的檢查

1. 它服務哪一個目標？講不出來就不開。
2. 目標 1 的卡：**執行者是誰？** 答案若是「靠人記得」，它其實是目標 3。
3. 目標 3 的卡：**現在有人因它受害嗎？** 沒有就進 Backlog，不排程。

## 1. 身分：記錄宣告，不追求驗證

**本 repo 的人類、PM、每個執行者、每個查核者共用同一個 GitHub 帳號 `ruan6047`。**

需求方 2026-08-12 裁定：

> 像是不知道是誰（GITHUB 帳號相同）**可以要求在欄位中追加身份來處理**。

**這是本檔前一版寫錯的地方。** 前一版把它當成「驗證身分」的問題，結論是所有身分檢查
永久 `structurally-vacuous`。**那是在為一個已被判為「非必要」的目標（可稽核的身分）
宣告無解，而真正的目標（可稽核的內容）只需要記下宣稱。**

### 裁定

需求方 2026-08-12 進一步界定，**系統需要知道的身分只有兩個維度**：

| 維度 | 值域 | 為什麼需要 |
|---|---|---|
| **角色** | 執行 / 查核 / PM / 管理者（人工） | 決定該筆留痕屬於哪個生命週期階段、誰不得自我批准 |
| **模型** | 模型家族與工具（如 `GPT-5@Codex`、`Claude Opus 5@Claude Code`） | 紅線卡的跨家族查核要求靠它成立 |

**不需要知道是哪一個執行緒或哪一次呼叫。** 需求方逐字：

> 驗證是哪個執行緒意義不大，因為**完成任務後才會提交文件**。
> 如果提供資料不完整可以退回。

交付是完成後才提交的產物；中間由哪個 instance 跑不影響那份產物能不能被稽核。

**因此執行面是「完整性檢查」而不是「身分驗證」**——欄位有沒有填、角色是不是合法值、
模型有沒有寫。這是機械可判的，且落在目標 1／2 之內；「證明他真的是他」落在目標 3，不做。

- 該欄位**已經存在**：Project 的 owner 欄、commit 的 `Implemented-by` / `Planned-by` / `Requested-by`、
  review event 的 reviewer 字串。缺的是**要求它們被填、且角色與模型兩者都在**。
- **資料不完整即退回。** 這取代先前所有「想驗證身分」的機制。
- **不要寫看起來在驗證身分、實際恆真的條文。** 正解是**改成宣告欄位＋完整性檢查**，
  不是把恆真性導出成 `structurally-vacuous` 再繼續假裝那是檢查。
- 代貼他人裁定時，**在留言內註明代擬代貼與授權來源**——那就是角色欄位的散文形式。

### 受此收斂的卡

`WF-22-CLI4`（#9）、`WF-AMEND-AUTHZ-BINDING1`（#62）、
`WF-ESCALATION-RESOLUTION-GAP1`（#39）的授權款——**三張都不必再追求驗證**，
改為確認宣告欄位存在且必填。

## 1.5 可攜的宣告 vs 機器局部的探測（2026-08-13 裁定）

**§1 那個形狀出現第三次了，所以它不是身分特有的，是一類。**

| # | 追的是 | 而目標只需要 | 卡 |
|---:|---|---|---|
| 1 | 驗證「他真的是他」 | 記下**宣稱**（角色＋模型） | #9、#62、#39 |
| 2 | 路徑指向的 repo 是什麼 | 這張卡**宣稱**屬於哪個 repo | #57 |
| 3 | 建立行為綁不綁得住 | （同上——`assign` 時那個事實還不存在） | #57 自陳 |

**共同形狀**：把一個**可攜的宣告**與一個**機器局部／時點局部的探測**混在同一條判定軸上，
然後追探測那一端——而探測那一端結構上拿不到。

### 觸發本裁定的查證（需求方 2026-08-13 提問後 PM 實跑）

> 絕對路徑是否是無效資訊，尤其是不同台電腦路徑不會完全相同。

- Project #4 全量：**絕對路徑 51 筆、相對路徑 18 筆**；相對路徑中 cpbl-analytics **16** 筆、
  ai-workflow **2** 筆。「新的 assign 一律給絕對路徑」這條 2026-08-12 的慣例，
  **實際上只約束了 PM 自己在打的那一半**。
- **相對路徑比絕對路徑更接近可稽核的形式**——`.claude/worktrees/xxx` 在任何 clone 上指向
  同一相對位置；`/Users/ruanruan/Dev/…` 只在單一台機器成立。**先前收緊的方向，
  收緊的是比較不可攜的那一種。**
- `probe_worktree_repo` 從路徑讀 `commondir` 反推 repo。**換一台機器，`target_dir` 探測必然失敗**，
  落到 `ancestor_dir` 或 `undeterminable`。故枚舉器的判定是**「這台機器的判定」，不是 repo 的事實**。

### 裁定

**欄位若同時承載可攜的宣告與機器局部的操作細節，判定必須建立在可攜的那一半上。**

以 `分支worktree` 為例：

| | 性質 | 誰需要 |
|---|---|---|
| **repo slug** | 可攜、可稽核、跨機器成立 | 歸屬判定、對帳、查核 |
| **本機路徑** | 機器局部、操作用 | `cleanup` 刪 worktree、`doctor` 探測 |

⚠️ **這不是「路徑無效」。** `cleanup.py` 用它做破壞性收尾（`status --porcelain` 檢查乾淨、
`resolve()` 後刪除），`doctor`／`snapshot`／`handoff` 亦讀它。**是「兩者不該共用一條判定軸」。**

### 開卡與審查時的檢查

在 §0 的三個檢查之後加一個：

4. **這張卡的判定建立在什麼上？** 若答案是路徑、目錄、本機檔案系統狀態、或任何
   「只在這台機器／這個時點為真」的東西——**先問可攜的那一半是什麼，判定能不能改建在它上面。**

**這一條沒有機械執行者。**

## 2. 唯一的執行面：CI

**2026-08-12 之前本 repo 完全沒有 CI**（`.github/` 不存在）。canonical 多處寫「守衛必紅」，
而那些守衛不存在——例如 `AI_WORKFLOW.md:221` 的 commit trailer 守衛，實測零命中。

**後果當日實現**：連續合併三張卡、每次只跑 `git merge-tree` 確認文字無衝突並在分支自己的基線上跑測試，
從未在合併後的結果上跑過；`WF-CLEANUP-GUARD1` 的基線早於能力旗標改必填的那次合併，
於是它自己的工作樹 388 passed 為真、併進 main 卻 14 個 error。

**裁定**：

- **`DEV-AIWF-MINIMAL-CI1`（#48）是唯一有可能成為執行面的東西**，優先度最高。
- ⚠️ **但它本身擋不了任何人。** 本節前一版寫「#48 是唯一會真的擋人的東西」——**那句是假的，
  而且本卡無法讓它變真**（#48 執行者 2026-08-12 指出，PM 複驗接受）。CI 產生的是**紅叉**；
  紅叉要變成**閘門**，需要 repo 的 `required_status_checks` ruleset，而
  **repo setting 不是檔案、不在資源模型的值域裡、不可能被宣告進任何寫入集**。
- **牙齒長出來的時點是 ruleset 套用那一刻**，不是 #48 合併那一刻。**該時點已於 2026-08-13 到達**：
  ruleset `id 20768920`（`~DEFAULT_BRANCH`、`bypass_actors: 0`、`strict: true`、required check `tests`）
  已套用生效。閘門在 PR #71 上實際運作過一次：`BEHIND` → `update-branch` → 兩筆 check 綠 → `CLEAN` → 才合併。
  ⚠️ **驗證的限度**：#61 關閉時 `mergeStateStatus` 是 `DIRTY`（base 前進造成衝突）而非 `BLOCKED`，
  故**未取得「紅色 check 直接擋下合併」的直接證據**，取而代之的是設定面證據。需求方裁定接受。
- 其餘全是**偵測器**（`doctor`、對帳器、枚舉器）。偵測器讓缺失可被列舉，**不阻止任何人 push**。
- **偵測器的卡不得宣稱「已預防」**——`WF-WORKTREE-REPO-OWNERSHIP1`（#57）的 R1-01 正是因此被判 blocking。
- 需要牙齒的偵測器，**等的是上面那第 3 步**，不是 #48 的 merge。

### ⚠️ 一個被實測推翻的前提：runner 不是 UTF-8

PM 2026-08-12 曾判斷「CI 跑在 ubuntu 的 UTF-8 locale，所以本機 C locale 驗過的腳本
不保證在 CI 上跑得起來」。**方向反了。** #48 執行者把 image 預設值印進 log 實測
（run `31612860735`，`env -u LANG -u LC_ALL locale`）：

```
LC_CTYPE="POSIX"    （其餘 LC_* 全為 POSIX）
```

**`ubuntu-latest` 預設是 C locale，與 PM 的本機同一側。** 落差要重述為：
**本機綠、CI 也綠、但在任何正常設定 locale 的機器上紅。**

不釘 locale 的話，**CI 只會把同一個盲點複製到一個大家更會信任的地方**。
已於 `ci.yml` workflow 層釘 `C.UTF-8`，並印出生效值 vs image 預設值——
因為 `C.UTF-8` 不存在時 glibc 會**靜默退回 POSIX**。

## 3. 現行排程

### 必要（依序）

> ⚠️ **本表前一版把 #43 與 #24 排成序 1、2 並寫成「是後面三張的前提」——那是錯的，
> 它們的碼在寫這份藍圖之前就已經在 main 了。** PM 憑印象排程、沒有先查狀態；
> 2026-08-13 依本節檢視時才發現。這與 PM 前一日三次「用記憶回答狀態問題」是同一個錯，
> 只是錯在規劃層而非報告層。**排程前先查狀態，這一條沒有機械執行者。**

| 卡 | 服務目標 | 狀態 |
|---|---|---|
| `WF-RESOURCE-BLOCK-ANCHOR1`（#43） | 1 | ✅ **已完成並收尾**（2026-08-13 補收結案債，見下） |
| `WF-RESOURCE-WRITESET1`（#24） | 1 | ✅ **已完成並收尾**（同上） |
| `DEV-AIWF-MINIMAL-CI1`（#48） | 1 | ✅ **已合併**（跨家族查核 APPROVE／0 blocking）。**閘門已於 2026-08-13 套用生效**，見 §2 |
| `DEV-COMMIT-TRAILER-GUARD1`（#63） | 2 | ✅ **已合併**（squash `d0397e0`，APPROVE／0 blocking） |
| `WF-WORKTREE-REPO-OWNERSHIP1`（#57） | 1 | 🔍 待查核（`544e162`，射程已縮為登記面） |
| `DEV-STATE-FACE-DRIFT-GUARD1`（#65） | 1 | 📥 已開卡未派——看板失真數小時無人發現；純比對、機械可判 |
| `WF-DISPATCH-FROM-HANDOFF1`（#66） | 2 | 📥 已開卡未派——派審詞與 handoff 兩個來源必然漂移，當日發作三次 |

### 2026-08-13 補收的結案債

#43 與 #24 的碼早已在 main，Log 亦已記 `owner —（結案）`，但**終態寫入了而收尾第 1–3 步一步都沒做**，
`cleanup.classify_state` 判 `illegal_terminal_before_cleanup`、守衛拒絕動作。

處置：**倒退交付狀態至 `📦已合併` → 由守衛執行第 1–3 步 → 終態重寫**。Log 依序記下
「終態寫入 → 更正倒退（附成因）→ 清理 → 終態重寫」——**那是準確的事件序，不是改寫歷史**。
兩張皆 `mode=applied`／`合法=True`，worktree 與本地／遠端分支已刪。

⚠️ PM 一度把這個處置說成「說謊」而傾向留著不動。**那個判斷是錯的**：真正說謊的是讓看板顯示
🏁完成 而清理一步都沒做——那正是它們前一天的狀態。

### 2026-08-12 對執行中卡片的分類（需求方裁定）

| 處置 | 卡 | 依據 |
|---|---|---|
| **繼續**（屬必要集合） | #48、#63、#57 | §3 排程內 |
| **當前輪次跑完即停，轉 Backlog** | #42、#52、#38、#39 | 屬目標 3。#38 很可能被 #66 取代，由 #66 執行者裁定 |
| **依 §4 驗收** | #9 | 核心痛點已消除；三項自陳全屬「有最好」 |

⚠️ **#63 須縮回原射程**（統一命名＋機械檢查器）。它現有的「裁定既成歷史怎麼採認」
是 PM 依 #52 查核者的 disposition 加上去的——**正是 §5 禁止的形狀**（由 disposition
直接決定開卡與擴大射程）。

⚠️ **#9 的已知不合規**：它寫出的每一則 review event 缺 `review-escalation.md` §5:168 的
`preflight_passed: true`。事件、Log、stderr 三面已載明，但 `CONSUMER_CONFORMANCE.md`
的登記不在其寫入集。依 §4 判準「會不會讓留痕重建不出來」判為**不會**（欄位缺席本身有留痕），
故驗收，缺登記轉 Backlog。**這是 §4 的第一次套用。**

### 降級為 Backlog（有餘力再做）

`WF-EVENT-MARKER-V2-SCOPE1`（#35）、`WF-CARD-FIELD-CORRECTION1`（#37）、
`WF-DISPATCH-PRECHECK1`（#38）、`WF-ESCALATION-RESOLUTION-GAP1`（#39）、
`WF-EVENT-IDEMPOTENCY1-ADAPT-STALE1`（#41）、`WF-CONTROL-PLANE-TYPE-REGISTRY1`（#42）、
`WF-RECONCILE-CLEANUP-GUARD1`（#45）、`WF-24-EVIDENCE-STRENGTH1`（#11）、
`WF-ORCHESTRATION-RECONCILE1`（#16）、`WF-MARKER-SCOPE-CLEARANCE1`（#30）、
`OPS-MIG1-CLAIMS-BACKFILL1`（#31）、`WF-EVENT-IDEMPOTENCY1-PROBE-CMD-DRIFT1`（#52）、
`WF-CLI-RESUME1`（#54）、`WF-CLI-MERGE1`（#55）、`WF-CLI-EPOCH-ANCHOR1`（#56）、
`WF-EVENT-TYPE-REGISTRY-RECONCILE1`（#58）、`DEV-CLI-VERB-WIRING1`（#60）、
`WF-AMEND-AUTHZ-BINDING1`（#62）

**降級不是關閉。** 它們載有真實 finding 的紀錄，關掉會讓那些發現消失；降級可逆，關閉不可逆。

**#42／#58 特別說明**：它們解決「兩份事件型別語彙互不知情」，而該分歧**今天 0 寫入端 0 讀取端、
不可能造成錯誤裁決**（#58 執行者自己的論證）。這是「有最好」的典型。

## 3.5 合併方式：一律 squash（2026-08-13 裁定）

### 問題

`AI_WORKFLOW.md §6:222` 要求 merge commit 帶 `Reviewed-by`，而 **GitHub 的 merge 按鈕
結構上產不出這一行**。`TRAILER_GUARD_EPOCH`（`2026-08-13T00:00:00+08:00`）跨過之後，
**每一次以 merge 按鈕合併的 PR 都會是一筆違規**——實測 `0ea7aba` 與 `dbf18d7` 的
`git interpret-trailers --parse` 解析出**零個** trailer。

`required_status_checks` 的 `strict` 政策使這件事無法迴避：落後的分支必須先更新，
而更新會產生 merge commit。

### 裁定

**卡片一律以 squash 合併。** 依據是實測而非偏好：

- `d0397e0`（#63 的 squash commit）**自己帶齊五行 trailer 含 `Reviewed-by`**——
  是第一筆會通過它剛落地的檢查器的 main commit
- **`gh pr update-branch` 產生的 merge commit 只存在於 PR 分支上，squash 會把它壓掉**，
  不落 main。既滿足 strict 政策，又不製造違規
- 不需要改 `TRAILER_GUARD_EPOCH`，也不需要動已通過查核的碼或 canonical

### 代價，不掩飾

**被審 SHA 不會出現在 main 的歷史上**，查核留痕與 main 的 commit 對不起來。
這對目標 2（可稽核的內容）是實質損失。

**緩解**：squash 訊息**必須逐字記下被審 SHA**，以及該輪查核的結論。今天 `d0397e0`
即為範例。**這是約定，沒有機械執行者。**

### 明確不涵蓋

- `§6:222` 的文字**不改**。squash 之後沒有 merge commit 落 main，該條的觸發條件消失，
  改文字是解一個已經不會發生的問題。**若日後恢復 merge 合併，該條重新生效。**
- 本裁定只約束**卡片合併**。分支內部的整合行為不受限。

## 3.6 收尾與排程（2026-08-13 裁定）：本 repo 轉入低頻維護

需求方 2026-08-13 逐字裁定：

> 我想把問題處理好後，其他項目幫忙規劃排程可以逐步補完，**回歸 CPBL 專案開發，這邊花太久了**。

**本 repo 自即日起轉入低頻維護。** 剩餘 Backlog **全部是「有最好」**（§0 目標 3），
以下分批**只給觸發條件，不給日期**——因為主線回到 CPBL 之後不會有固定節奏。

### 批一：有人受害時才做

| 卡 | 觸發條件 |
|---|---|
| `DEV-STATE-FACE-DRIFT-GUARD1`（#65） | **開始多 agent 並行**，或再次發生「看板與真實不符數小時無人發現」 |
| `WF-DISPATCH-FROM-HANDOFF1`（#66） | 再次發生派審詞與 handoff 的 SHA 漂移 |

**這兩張對應的是 2026-08-12–13 PM 犯錯最多的兩個面**（五次漏跑 `handoff`、三次派審詞 SHA 漂移）。
但**只要沒有多 agent 並行就不痛**——單線操作時人會發現。

### 批二：下次開新動詞卡時順手做

`WF-CLI-RESUME1`（#54）、`WF-CLI-MERGE1`（#55）、`WF-CLI-EPOCH-ANCHOR1`（#56）、
`DEV-CLI-VERB-WIRING1`（#60）。

**它們互相成組**：#60 是前三張的接線，而 #53 已把註冊點收斂成 append 一行。
**單獨做任何一張都划不來。**

### 批三：明確不做，除非目標改變

| 卡 | 不做的理由 |
|---|---|
| `WF-CONTROL-PLANE-TYPE-REGISTRY1`（#42）／`WF-EVENT-TYPE-REGISTRY-RECONCILE1`（#58） | 該分歧**今天 0 寫入端 0 讀取端**，不可能造成錯誤裁決 |
| `WF-ORCHESTRATION-RECONCILE1`（#16） | 三個機制本體已落地，框架卡剩餘價值未經證實 |
| `WF-ESCALATION-RESOLUTION-GAP1`（#39） | 授權款依 §1 收斂為宣告欄位，其 `structurally-vacuous` 設計是過度工程 |
| `WF-WORKTREE-REPO-OWNERSHIP1`（#57） | **可攜軸在 64 筆真實資料上 block 0**；能擋的抓不到、抓得到的（機器局部軸）依 §1.5 不准擋 |

**這四張都證明了它們追的東西結構上拿不到，或今天沒有消費者。**

### 收尾時發現、未處理的三項已知限制

**一、降級為 Backlog 會讓該卡的 worktree 被 `doctor` 判為孤兒。**
2026-08-13 降級 20 張後，`doctor` 報「20 個 worktree、17 個孤兒」，而那些卡的
`分支worktree` 欄**明明還有值**——成因是 `doctor` 只掃活卡，Backlog 不算活卡。

⚠️ **後果不是噪音而已**：孤兒清單是用來找真的該清理的東西的，**17 筆假陽性會把真的淹掉**。
而**兩個真實跨 repo 漂移 worktree 本來就不在這份清單裡**（兩個 repo 的 `git worktree list` 都 0 命中）。
承接者的自然人選是 `#65`——同一類問題：狀態面與真實不符而沒人發現。

**二、`doctor` 的「可安全清理」判定與 `merge-base --is-ancestor` 不一致。**
它對 `docs/merge-policy` 判「已完全併入 main，可安全清理」，而該分支是 **squash 合併**的、
tip 不是 main 的祖先。PM 自己多驗一次才沒照它的建議刪。**這與 `WF-CLEANUP-SQUASH-AWARE1`（#78）
是同一類問題的另一面**——一個過寬、一個過嚴。#78 落地時應一併檢視。

**三、`wfcli review` 寫出的每一則事件都缺 `review-escalation.md` §5:168 的 `preflight_passed: true`。**
這是 #9 裁定的直接後果，事件／Log／stderr 三面已載明，但 `docs/CONSUMER_CONFORMANCE.md`
的登記**沒做**（不在 #9 的寫入集）。

### 交回主線的判準

**本 repo 唯一還會擋住正常運作的是 `#78`**（收尾守衛對 squash 恆拒，之後每張卡都收不掉尾）。
它落地並收掉 `#9`／`#63`／`#73` 三張的尾之後，**本 repo 即可放著**。

**一句話**：這套治理系統現在會擋壞碼、有 trailer 檢查器、藍圖記得為什麼做每張卡——
**它已經比 CPBL 本身被照顧得更周到了，而 CPBL 才是有使用者的那個。**

## 4. 驗收政策（2026-08-12 裁定）

> 已做完的項目如果大項目沒問題先驗收，後續細節轉成卡片之後進行。

**大項目通過即可驗收**，不因細節 finding 而整張退回。細節轉為 Backlog 卡。

### 判準

| 情況 | 處置 |
|---|---|
| `core_pain_resolved: no` | **退回**。核心痛點未消不適用本政策 |
| `core_pain_resolved: yes` ＋ blocking 全屬細節 | **驗收**，細節開 Backlog 卡 |
| blocking 指向**會造成低級事故**的缺陷 | **退回**，不論核心痛點判定 |
| blocking 指向身分不可稽核 | 引用 §1，**不構成退回理由** |

**「細節」的判準不是嚴重度標籤，是後果**：它會不會讓某人合併壞碼、改錯檔、或讓留痕重建不出來。
會 → 不是細節。

### 這條政策改變了什麼

先前每一個 blocking 都讓卡再走一輪，於是查核越認真、輪次越多、卡越開越多。
2026-08-12 當日開 20 張、結 4 張——**發散**。本政策是收斂機制。

## 5. finding 的處置：先看整體規劃，不是看一個修一個

需求方 2026-08-12 裁定：

> 應該以整體規劃來排任務，而不是看一個修一個。發現問題應該**先檢視整體規劃**，
> 除非是嚴重 BUG 才要緊急修，否則應該視為**需求任務**。

**這是 2026-08-12 發散的直接成因**：每一個 finding 被當成一張卡，於是查核越認真、
卡越多，而沒有任何一步問過「這件事在整體規劃裡的位置是什麼」。

### 收到 finding 時的處置順序

1. **它會不會現在就造成低級事故？**（壞碼進 main、改錯檔、留痕重建不出來）
   → 是：**緊急修**，不排隊。
2. **不是的話——它服務 §0 的哪個目標？**
   → 目標 3：**記錄，不開卡**。等它真的害到人再說。
   → 目標 1 或 2：進入第 3 步。
3. **它與 §3 的排程衝突嗎？**
   → 衝突：**先改排程再動手**，不要插隊。排程是需求方的，不是 finding 提出者的。
   → 不衝突：併入既有卡，或開卡並在 §3 登記。

### 明確禁止

- **不得因為 finding 存在就開卡。** finding 是觀察，不是任務。
- **不得由查核者的 disposition 直接決定開卡。** disposition 描述「該怎麼修」，
  不決定「該不該現在修」——後者是需求方依整體規劃裁定的。
- **不得把「查核者要求了」當成排程理由。**

## 6. 本檔的維護

- 開卡時在此確認它服務哪個目標；找不到就不開。
- 降級或升級卡片時更新 §3。
- **需求方裁定的目標排序（§0）只有需求方能改。**
