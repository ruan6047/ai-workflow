# WF-21 審核退回分流與升級計數去誤觸發〔T4；🔴規則正確性〕

- 需求：ruan6047（2026-07-30 指示直接修改）　規劃：GPT-5@Codex　分支：`ai/gpt-5@codex/WF-21`
- 執行：GPT-5@Codex（建議 L3；跨 canonical／adapter 的流程語意）　查核：待指派（須跨模型家族或 ruan6047 sign-off）
- Initiative：—　spec 基線：本卡
- DB：`none`
- 部署：否　環境：—　PR：—　Merge SHA：—
- 範圍：canonical §3／§4.1／§5、control-plane template 與新增 review escalation template
- Discovery：cpbl-analytics 2026-07-30 event log 稽核；已證實卡面 metadata、重複同 SHA、前置未完成與實作缺陷共用 `REJECT` 計數。
- Design：Design Gate N/A；純規則與 adapter 契約，不改產品 UX。

## 驗收條件

- [x] 送審前失敗、阻塞、無效查核與實質 request-changes 有互斥定義。
- [x] 同一 source SHA 的多 reviewer 結果最多計一次 escalation attempt。
- [x] Coordinator／卡面治理／外部環境缺陷不消耗執行者的升級額度；權威交付文件的實質錯誤仍可計入。
- [x] 三次不同且持續收斂的 finding 不再自動轉 `🚨已升級`；重複根因或未修 blocking finding 仍 fail-loud。
- [x] 重規劃／換執行者以新 escalation epoch 計數，歷史事件不改寫。
- [x] finding 的採認、閉合與根因有機器可讀欄位；`unknown` 不得被當成共同根因。

## 驗證

- [x] `git diff --check`
- [x] canonical §3、§4.1、§5 與 templates 用語一致。
- [ ] 跨模型家族 review 或 ruan6047 sign-off。

## Log

- 2026-07-30：GPT-5@Codex 依需求方指示開始 iteration 0；規則稽核來源為 cpbl-analytics 既有 append-only review／escalate events。
- 2026-07-30：執行中反向稽核發現草稿缺 `accepted`／`status` 欄位，且把所有同 SHA 後續 reviewer 都當無效，皆與原驗收衝突；已改為必要多 reviewer 合併成同一 attempt，並以穩定 finding ID 追蹤採認與閉合。
- 2026-07-30T18:51:22+08:00：執行交付 `725af8185737ee07dbf77b551f8000ee0a015d39`；`git diff --check` 通過，canonical §3／§4.1／§5 與 template 用語對帳完成。因本卡為 T4 規則正確性，轉待跨模型家族查核或需求方 sign-off；執行者自查不充當獨立審核。
- 2026-07-30：adoption 反向測試發現「有 checkpoint」仍不足以保證原設計；若 adapter 信任手填 decision，三次同根因仍可填 `continue`。契約補明 adapter 必須跨 attempt 推導重複根因與未閉合 finding，違規 decision fail loud。
