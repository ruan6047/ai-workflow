# CLAUDE.md — ai-workflow 專案 AI 運行準則

本 repo 是**跨專案 AI 協作治理的 canonical 來源**。它**受自己定義的機制管理**（dogfooding）。

> **規則本體＝ [`AI_WORKFLOW.md`](AI_WORKFLOW.md)**（唯一權威）。本 repo 的任務看板／log＝[`TASKS.md`](TASKS.md)。

## 在本 repo 工作時
- 改規則（`AI_WORKFLOW.md`）＝一張任務卡：**開分支 `ai/<模型@工具>/<卡ID>` → 獨立審核（≠ 執行者）→ merge main**。規則屬「錯了影響全專案」→ 視為 **🔴紅線**，審核**必換模型家族或使用者 sign-off**。
- 規則更新後**不需同步各專案**（各專案只放指向本檔的 stub、不複製全文）。
- commit 一律加 trailer：`Requested-by / Planned-by / Implemented-by / Reviewed-by`（見 AI_WORKFLOW §6）。

## 模型路由
卡面「建議能力層級」只有三個合法值，開卡時由 `wfcli` 機械強制（`card.CAPABILITY_TIERS`；`open --exec-capability` 必填且限定選項）：**經濟型／主力型／高階型**。本 repo 幾乎全是文件治理，判準逐條寫在下面——⛔ 讀者不需要跳到別的檔就判得出來：

- **經濟型**：純機械文字。格式、錯字、狀態同步這類「改錯了看一眼就知道」的工作。
- **主力型**：一般修訂與實作。要讀懂既有條文才改得動，但語意影響仍收斂在單一檔或單一動詞內。
- **高階型**：動到規則本體（`AI_WORKFLOW.md` 的條文）、跨模組語意、根因不明、或不可逆。

⚠️ 選層看**風險**不看**工時**：多日但重複的工作仍是主力型；一小時但會改變規則語意的是高階型。
⛔ 不得因當下額度預先把建議寫小——建議欄記的是任務風險本身；派工時若偏離，偏離與實際模型記進 claim 事件。
規則正確性屬紅線：不論選哪一層，查核**必換模型家族或由使用者 sign-off**。

> ⚠️ **本節 2026-08-27 整段改寫**（`DOC-STALE-FILE-LINE-POINTERS1`，需求方裁定甲案）。舊寫法用四個代號分級、並要讀者跳去 canonical 的一個節次查完整表，而那個節次 2026-07-15 就被搬走，指標從此懸空。**這是刻意的三件事**：(a) 現在的行為是本節自足、不轉指；(b) 為什麼——轉指的那一跳正是會腐爛的東西，而從那天起到本次改寫為止沒有人回頭找它（⛔ 不寫「爛了幾週」——那個數字每天都不一樣）；(c) ⛔ 不得推出「分級只剩三級是暫時的」——舊的第四級（「多日或極難」單獨一級）**今日的卡面沒有對應值可填**，那類工作一律落**高階型**；也 ⛔ 不得把舊代號或那個節次引用寫回來。
