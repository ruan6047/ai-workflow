# 交付報告範本 (Delivery Report)

> 承載 `stage-rules/executor-conduct.md` 一與 `stage-rules/implementation.md` §3 的 ③。**報告是 ③ 的一部分**：只推 commit 沒報告＝仍在進行中，⛔ 不得轉待確認。自評只有本人寫得出來——PM 代寫的是另一份文件（他人所寫者逐字標「複驗報告」，⛔ 非自評）。
> 信封四段的權威定義在 [`handoff-contract.md`](handoff-contract.md) §3.3，⛔ 本檔不重寫定義。
> ⚠️ 看板值仍為舊語彙（15 值），對照見決議 §一；切換於切換 Initiative。

## 信封一 · 卡與身分

- 卡ID／Issue：`<CARD_ID>`（`<owner/repo>#<n>`）　級別：`<T0–T4>`　Initiative：`<父卡／—>`
- spec 基線：`<spec_version>`　階段：`執行`　輪次（iteration）：`<n>`
- from：`<執行者 模型@工具>`　to：`<PM 帳號>`
- 核心痛點（卡面原文逐字）：`<原文>`
- **交付入口 SHA**：`<完整 40 碼；git rev-parse 取，⛔ 不手打>`　分支：`<branch>`
- 模型：實際 `<模型@工具>`　卡面建議 `<經濟型／主力型／高階型>`　偏離理由 `<相符時填「相符」>`

⚠️ **入口 SHA 綁定**：本欄的值必須等於 PM 接著要 handoff 的 `source_sha`。報告寫完後又落 commit ⇒ 報告入口與分支 head 差一，查核者會拿到兩個互相矛盾的 SHA ⇒ PM 初審逕行退回。

## 信封二 · 身分自述

- 撰寫者 GitHub 帳號：`<帳號>`
- session ID：`<Claude：~/.claude/projects/<cwd>/<id>.jsonl 的 <id>；Codex：rollout-<時間>-<id>.jsonl 的 <id>>`
- 該則訊息定位：`<Claude：訊息 uuid；Codex：該則 timestamp>`

## 信封三 · 機械指令

> `self_run`：**實跑**紀錄，⛔ 不讀碼推論。rc 分開取、⛔ 不接管線、⛔ 不截斷輸出。

| # | 指令 | rc | 觀察到的輸出（原始，⛔ 不摘要） |
|---|---|---|---|
| 1 | `<指令逐字>` | `<rc>` | `<數字／關鍵行>` |

- ⚠️ `rc=0` ⛔ 不等於成功——判成敗看**被改變的狀態**。宣告成功前先核那次執行的識別碼。
- 「全部／全數」須附窮舉證據，由 artifact 自動產生；artifact 須在交付入口 SHA 可重現。

## 信封四 · 已知未驗項

> 逐項＋各自原因，⛔ 不摘要、⛔ 不加緩和語、⛔ 不裸列。三分類擇一。

| # | 未驗項 | 分類 | 原因 |
|---|---|---|---|
| 1 | `<項目>` | `驗不了`／`沒去驗`／`刻意不驗` | `<刻意不驗須寫委給誰及理由>` |

---

## 1. 做了什麼

- 本輪 commit 清單：`<SHA 逐筆＋標題；git log 取>`
- 改動面：`<檔案；每檔一列>`
- ⭐ 交付物寫**事實**⛔ 不寫狀態——SHA／數字可寫；「已 push」這類可變狀態一律寫**查詢方法**。

## 2. 逐驗收條件證據

> 一條 AC 一節，⛔ 不合併。每節必須指得出「什麼結果會推翻它」。

### AC`<n>`：`<條件逐字抄自卡面>`

- 做法：`<一句話>`
- 證據（指令＋原始輸出）：`<逐字>`
- falsifier：`<什麼觀察會讓本條不成立>`

## 3. 失誤登記

> **逐項**轉錄，⛔ 不摘要、⛔ 不加緩和語（把 blocking 講成 caveat 比漏掉更糟）。本輪沒有就逐字寫「無」，⛔ 不留空。

| # | 失誤 | 何時發現 | 影響 | 已做的補救 |
|---|---|---|---|---|
| 1 | `<逐字>` | `<步驟>` | `<範圍>` | `<動作；未補救寫「未補救」＋原因>` |

## 4. 注意事項回應清冊

> 對派工包 ① 印出的**三層編號清單**（`F-<階段>-NN`／`P-`／`T-`）**逐條**回應。**格數不符＝退回**；④ 只機械對照格數與值域，⛔ 不判內容。

| 編號 | 回應（三值擇一） |
|---|---|
| `F-<階段>-01` | `已遵循`／`不適用：<原因>`／`發現：<處置>` |

## 5. 13 族踩坑清冊

> 族名與範本由 `wf_cli.pitfalls` 取，⛔ 不手打：
>
> ```bash
> cd cli && uv run python -c "from wf_cli import pitfalls; print(pitfalls.report_template('<階段>'))"
> ```
>
> 每族**恰好一行**，三值擇一：`已檢查`／`不適用：<原因>`／`發現：<處置>`。`已檢查` 裸寫，說明進 evidence，⛔ 不升成 `發現：`。⚠️ 本清冊的三值與 §4 注意事項回應清冊的三值**不同**（`已檢查` vs `已遵循`），⛔ 不得互相代用。

```text
<把 report_template() 的輸出貼進來逐行填值>
```

## 6. 待需求方裁決

> 範圍外發現只能是本節的內容。⛔ 不自行開卡、⛔ 不 spawn 背景任務、⛔ 不建背景待辦。

- `<逐條；無則逐字寫「無」>`

## 7. 高階型研究卡加項

- 可重跑 harness：`<路徑＋跑法>`——查核者的 ≥3 個不同族角度對抗性反測靠它跑。
- ⛔ 非高階型研究卡時逐字寫「不適用：本卡非高階型研究卡」，⛔ 不刪本節。
